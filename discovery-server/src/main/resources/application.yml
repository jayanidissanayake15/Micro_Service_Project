server:
  port: 8761

spring:
  application:
    name: discovery-server

eureka:
  instance:
    prefer-ip-address: true
    instance-id: kaveesha-server:discovery-server:8761
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    enable-self-preservation: false
